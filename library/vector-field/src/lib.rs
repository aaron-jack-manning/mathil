use mathil::{
    colours::*,
    maths_objects::*,
    rendering::*,
    utilities::*,
    animation::*,
    errors::Error,
};

fn draw(points : Vec<Point>, field : fn(Point) -> Point) {


    // cartesian plane by min and max of x and y

}

fn square_point_array() {

}
